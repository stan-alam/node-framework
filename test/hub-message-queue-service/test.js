"use strict";

const globalTest = require('../../globalTest');

describe('Starting Hub-message-queue-service End to End', function() {
    globalTest.runTestFramework('hub-message-queue-service');
});
